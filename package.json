{
  "name": "docusign-sf-integration",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": "^9",
    "@sap/xssec": "^4",
    "axios": "^1.18.1",
    "@cap-js/sqlite": "^2.4"
  },
  "devDependencies": {
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "private": true,
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": { "url": ":memory:" }
      },
      "[production]": {
        "auth": "xsuaa"
      }
    }
  }
}